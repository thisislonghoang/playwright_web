//Test case kiểm thử cho dashboard

import { test } from '@playwright/test';
import { DashboardPage } from '../src/utils/pages/dashboardPage';

/*test('Kiểm tra tiêu đề trên trang Dashboard', async ({ page }) => {
  const dashboardPage = new DashboardPage(page);
  
  await dashboardPage.verifyDashboardHeader(); // Kiểm tra tiêu đề dashboard
});
*/